// candidate.controller.ts

import { Request, Response, NextFunction } from 'express';

// Existing code...

public async getDomains(req: Request, res: Response, _next: NextFunction): Promise<any> {
  // Function implementation...
}

// Existing code...
